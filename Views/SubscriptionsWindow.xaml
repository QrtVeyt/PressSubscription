<Window x:Class="PressSubscription.Views.SubscriptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подписки"
        Height="400"
        Width="700">

    <DockPanel>

        <Button Content="Добавить" Click="Add_Click" DockPanel.Dock="Top"/>

        <DataGrid x:Name="SubscriptionsGrid" AutoGenerateColumns="True"/>

    </DockPanel>

</Window>